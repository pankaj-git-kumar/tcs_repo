name: Verify Email Step

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3003123@ds.study.iitm.ac.in
        run: echo "Hello, world!"
        
